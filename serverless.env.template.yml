local:
  STAGE: local
  REGION: localhost
  DEBUG: true
  TABLE_PREFIX: ${env:LOCAL_TABLE_PREFIX}
  DYNAMODB_ENDPOINT: ${env:LOCAL_DYNAMODB_ENDPOINT}

test:
  STAGE: test
  REGION: localhost
  DEBUG: true
  TABLE_PREFIX: ${env:TEST_TABLE_PREFIX}
  DYNAMODB_ENDPOINT: ${env:TEST_DYNAMO_ENDPOINT}

dev:
  STAGE: dev
  REGION: ${env:DEV_AWS_REGION}
  DEBUG: true
  TABLE_PREFIX: ${env:DEV_TABLE_PREFIX}

stg:
  STAGE: stg
  REGION: ${env:STG_AWS_REGION}
  DEBUG: false 
  TABLE_PREFIX: ${env:STG_TABLE_PREFIX}

prd:
  STAGE: prd
  REGION: ${env:PRD_AWS_REGION}
  DEBUG: false
  TABLE_PREFIX: ${env:PRD_TABLE_PREFIX}